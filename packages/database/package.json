{
  "name": "@intemso/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'database package is consumed as source'",
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "seed": "tsx src/seed.ts",
    "studio": "prisma studio",
    "lint": "echo 'no lint configured yet'",
    "clean": "echo 'nothing to clean'"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  },
  "devDependencies": {
    "prisma": "^6",
    "tsx": "^4"
  }
}
